// Ejercicio 2: En este ejercicio, se define una función que recibe un argumento y se muestra en la consola
// el tipo de dato utilizando el operador typeof.

function tipoDeDato(valor) {
    console.log(`El tipo de dato de ${valor} es: ${typeof valor}`);
}

tipoDeDato(true);